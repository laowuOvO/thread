1、进程：受操作系统管理的基本运行单元
2、线程：在进程中独立运行的子任务
3、cpu一个核同时只能运行一个线程
4、一个进程中至少有一个线程正在运行
5、常用方法
    currentThread() 返回那个线程正在被调用
    isAlive() 测试线程是否处于活动状态
    getId() 获取线程唯一标识

    ----------------
    第一种：线程执行完run()后会终止

    第二种：
        wn  stop最好别用 虽然确实可以停止一个线程 不安全 而且已经被废弃了
        stop()停止线程
    第三种：
        wn  虽然名字是终止的意思 但是这个方法不会真正终止一个线程
        wn 只是在线程中打了一个标记还需要加入一个判断才行 后面研究
        interrupt() 停止，终止
